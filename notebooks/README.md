# `notebooks/`

- **`EDA_basico.ipynb`** — EDA mínimo sobre los JSONL de HumanEval/MBPP (tras `python src/ingest_datasets_v0.py`). Abrir con Jupyter/VS Code y ejecutar desde esta carpeta o elegir el kernel de Python del entorno con dependencias instaladas.
- (Opcional más adelante) `Baseline_basico.ipynb` — cuando definas un baseline acorde a tu tarea.

Importar utilidades desde `../src/` cuando apliquen.
