# `data/processed/`

Datos listos para modelos, evaluación o análisis (salidas de preprocesamiento y uniones controladas). El repositorio de ejemplo del curso usa el mismo rol para almacenar el dataset transformado; aquí se poblará según el pipeline definido en `src/`.
